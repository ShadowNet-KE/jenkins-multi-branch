pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'ls'
            }
        }
        stage('Test') { 
            steps {
                sh 'df -h'
            }
        }
        stage('Deploy') { 
            steps {
                sh 'ls -lh'
            }
        }
    }
}
